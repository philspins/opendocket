## Active work
PEI scraper is broken — see docs/pei-scraper-context.md for full diagnosis.
Run `make crawler -- --provincial 2>&1 | grep pe` to test.

## Tool preferences
Prefer to use Go as a primary coding tool instead of Python